const authRepository=require('./auth-repository');
const globalRepository=require('./global-repository')
const contactRepository= require('./contact-repository')
module.exports={
    authRepository,
    globalRepository,
    contactRepository,
    
}